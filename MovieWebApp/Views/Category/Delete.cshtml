@model Category

<form method="post" asp-action="DeletePOST">
    <input asp-for="Id" hidden /> @*böylece kategori kimliği sunucuya gönderilirken gizlenir. Bu, kullanıcının kimliği değiştirmesini veya hatalı bir kimlik göndermesini engeller ve uygulamanın doğru kategoriyi silmesini sağlar.*@
    <div class="border p-3 mt-4">
        <div class="row pb-2">
            <h2 class="text-primary">
                Edit Category
            </h2>
        </div>
        <div asp-validation-summary="All"></div>
        <div class="mb-3">
            <label>İsim</label>
            <input asp-for="Name" disabled class="form-control" />
            <span asp-validation-for="Name"  class="text-danger"> </span>
        </div>
        <div class="mb-3">
            <label>Sıra</label>
            <input asp-for="DisplayOrder" disabled class="form-control" />
            <span asp-validation-for="DisplayOrder" class="text-danger"></span>
        </div>

        <button type="submit" class="btn btn-primary" style="width:150px">Sil</button>
        <a asp-controller="Category" asp-action="Index" class="btn btn-secondary" style="width:150px">Geri Dön</a>
    </div>
</form>

@section Scripts{
    @{
        <partial name="_ValidationScriptsPartial.cshtml" />
    }
}
